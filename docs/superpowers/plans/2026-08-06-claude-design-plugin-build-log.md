# SDD ledger — plan: docs/superpowers/plans/2026-08-06-claude-design-plugin.md

merge-base: fa4ca0cd73f8443cb71f9e0e686f0314661c45ce

Task 1: implemented (commit 10342cf) — spec ✅, quality approved
Task 1: finding (Important, plan-mandated) — test asserted output but not exit code; human ruled finding governs
Task 1: fix round 1/5 dispatched — add exit-code assertions both directions + non-vacuity proof
Task 1: fix round 1/5 (1 addressed, 0 open; commits 10342cf..3ab65c4)
Task 1: complete (commits b6cd388..3ab65c4, review clean)
Task 2: complete (commits 3ab65c4..a463fdb, review clean)
Task 2: minor (deferred): MOTION.md modal row 200-500ms exceeds the 300ms ceiling stated above it; resolved in-doc by 'tighter bound wins' but places inference on the scorer
Task 2: minor (deferred): test-standards.sh 'transform'/'opacity' checks use bare-word literals that match incidental occurrences (transform-origin, 'drop transforms') - deleting the actual animate-only rule sentence still passes. Needs an anchored assertion.
Task 3: complete (commits a463fdb..36f1178, review clean)
Task 3: minor (deferred): RUBRIC.md uses '0-5' hyphen (brief test line 25) while TYPOGRAPHY.md uses '0–5' en-dash (brief line 62) — brief self-contradicts; resolved toward the test
Task 3: minor (deferred): test-rubric.sh 'escalate'/'evidence' bare-word checks mutation-proven vacuous — reviewer reversed the rule's meaning and test still passed 25/25
Task 3: minor (deferred): RUBRIC.md forward-references TASTE.md (created later by /taste) — expected, not a defect
Fix (human-directed, commit 6757ac9): anchored 4 vacuous bare-word assertions to rule-specific phrases; controller independently mutation-verified the escalate check now fails on rule reversal (24/25) and restores clean. Resolves 2 of Task 2/3's deferred minors.
Decision: Tasks 4-9 tests must assert distinctive multi-word phrases unique to each rule, never bare words. grep -c uniqueness == 1 required.
Decision: Task 11 live cases run against the open Figma file 'Spry'; use figma-cli undo to clean up created frames.
Task 4: implemented (commit 856681c) — spec ✅, quality approved, 13 anchored assertions all grep -c==1
Task 4: fix round 1/5 dispatched — styling-paradigm anchor omitted 'Never', so reviewer flipped prohibition→permission undetected
Task 4: fix round 1/5 (1 addressed, 0 open; commits 856681c..101a8f2) — reflowed one FRAMEWORKS.md sentence so 'Never' shares a line with the prohibition; controller independently re-ran the reviewer's mutation, now correctly FAILs (12/13)
Task 4: complete (commits 6757ac9..101a8f2, review clean)
Task 5: complete (commits 101a8f2..07585a2, review clean — ZERO findings; 25/25 anchors grep -c==1, 3 independent mutations caught, polarity hunt found no gaps)
Task 6: complete (commits 07585a2..f0bd077, review clean) — gate-before-QA ordered pairs verified unique-hit at intended lines; 2 whitespace-only reflows in qa-brief.md proven meaning-identical; live Figma render correctly skipped and honestly reported
Task 6: minor (deferred): brief Step 6 says 'Expected: PASS=19' but its own script has 18 checks — brief off-by-one, implementer's 18 correct
Task 7: complete (commits f0bd077..48e7c69, review clean) — 4 ordered pairs heading-anchored; reviewer confirmed the brief's bare-word 'animate' check WAS vacuous (matched line 28 prose before pick-ui-library at 32), so anchoring is what makes the sequence enforceable
Task 7: fix round 1/5 (1 addressed, 0 open; commits 48e7c69..3578695) — library-first prohibition now covered; controller independently verified flipping 'Never hand-roll'→'Always feel free to' FAILs (24/25)
Task 7: complete (commits f0bd077..3578695, review clean)
Task 8: fix round 1/5 (3 addressed, 0 open; commits 2470a92..011557c) — SLOP check now ordered(), approach rule covered, qa-brief escalation aligned to RUBRIC ('same dimension ≤2 on two consecutive passes'); controller verified all 3 documents now agree
Task 8: complete (commits 3578695..011557c, review clean)
Task 8: minor (deferred): /review references TASTE.md for breakpoints without absence-handling (inherited from brief)
Task 9: fix round 1/5 (2 addressed + 1 self-found, 0 open; commits c93fe89..7097113) — image-input degrade half now guarded for Antigravity, Grok AND Codex (fixer's own mutation disproved the review's claim that Codex was adequate); controller independently verified inverting the rule now triggers 2 FAILs where it previously passed 22/22
Task 9: complete (commits 011557c..7097113, review clean)
Fix (controller-found, commit be04ec6): implemented the standard-resolution rule the spec required but no task built. Single-skill-install simulation showed explore/ and review/ ship with ONLY SKILL.md yet referenced skills/design/RUBRIC.md etc by dangling paths; ship/ referenced MOTION.md bare which resolved under NEITHER install path. All 5 SKILL.md now state the rule, self-prefixes dropped, siblings use <skill>/<FILE>, explore+review carry inline fallbacks. New tests/test-resolution.sh (34 checks) incl. mechanical on-disk dangling-ref scan.
Task 10: complete (commit c041cb5) — tests/run-all.sh reports 202 checks across 10 suites, ALL PASSED; claude plugin validate passes; both install formats verified (frontmatter complete on all 5 skills, no repo-root standards/, single-skill install simulated per skill)
Fix (live-validation-found, commit 6a220cd): playwright + figma-use optional deps missing, 4 documented commands failed opaquely, preflight checked neither. Now reported without failing the run; docs state the honest fallback (record inferred, not measured).
Fix (live-validation-found, commit 53313ae): SEVERE - var: references need SLASH form (neutral/900 per var list) but export css emits HYPHEN form (--neutral-900). Using the CSS form warns, renders grey placeholders, and STILL EXITS 0. Would have degraded every design silently. Documented in FIGMA-CLI.md, made a build failure in design/SKILL.md, reconciled in ship/emit.md.
Task 11: complete (commit 634c3f0) — live validation against Spry. PASSED: render --verify one-call screenshot, export css token fidelity, a11y contrast live findings, undo restores canvas (3 elements before and after). The visual pass caught a failure text output only warned about. NOT RUN with reasons recorded: cluster/responsive intake (missing deps), full agent-loop cases (behavioural), dual-distribution install (needs push).
Correction (final-review fix pass): Task 8's line 30 above ("controller verified all 3 documents now agree") was inaccurate. Only qa-brief.md had been aligned to "same dimension ≤2 on two consecutive passes" — RUBRIC.md:51 and review/SKILL.md:38 still said "any dimension," so the three documents disagreed the whole time despite the ledger's claim. This pass fixed RUBRIC.md, review/SKILL.md (both its process step and its inline single-skill-install fallback), design/SKILL.md, and design/RUNTIMES.md to all say "same dimension," added anchored assertions in test-rubric.sh, test-explore-review.sh (two anchors), test-design.sh, test-runtimes.sh, and test-resolution.sh guarding the same-dimension wording, and independently mutation-verified (RUBRIC.md reverted to "any dimension" → test-rubric.sh FAILs 30/1, was 31/0; restored → clean).
Fix (final whole-branch review, this commit): addressed 6 numbered findings plus 2 Emil-orchestration spec-delivery gaps. (1) tests/test-resolution.sh's dangling-ref scan globbed only skills/*/SKILL.md, so 4 broken bare `MOTION.md` refs in supporting files (ship/states.md, ship/FRAMEWORKS.md, taste/TASTE-template.md, taste/intake.md x2) passed 218/218 undetected — scan extended to every markdown file under skills/, all 5 dangling refs corrected to `design/MOTION.md`, mutation-verified (break one → FAIL, restore → clean). (2) FIGMA-CLI.md:87 `spec --check <nodeId> --tolerance 2` fails live with "missing required argument 'component'" (verified against `figma-cli spec --help`); corrected to `spec <component> --check <nodeId> --tolerance 2`, matching gates.sh and design/SKILL.md's already-correct usage. (3) see correction paragraph above. (4) README's "nothing hard-depends on scripts/" claim made true: design/SKILL.md and taste/SKILL.md now document preflight.sh's checks inline (figma-cli --version/status, emil-design-eng dir check, playwright/figma-use optional-dep checks), design/SKILL.md documents gates.sh's three composed calls run directly. (5) review/SKILL.md now degrades to default breakpoints 390/834/1440 with a stated notice when TASTE.md is absent, matching /explore's pattern. (6) MOTION.md's 300ms-ceiling/modal-range ambiguity resolved explicitly: modal/drawer's exception applies only to a full-viewport transition, other elements remain bound by both their own range and the 300ms ceiling regardless — no numbers changed. Plus: apple-design wired into taste/interview.md (motion-appetite question) and ship/SKILL.md (step 5, gated on TASTE.md calling for native feel); animation-vocabulary wired into taste/interview.md's motion-appetite question as the vague-language translator the spec assigns to /taste. tests/run-all.sh: 218 → 250 checks across 10 suites, all passing; claude plugin validate passes; scripts/preflight.sh exits 0; single-skill install re-simulated for explore/ and review/, both self-sufficient on inline fallbacks with no dangling references.

FINAL REVIEW (opus, 21 commits/36 files): ship after fixing 4 findings. Found a vacuous assertion the controller missed — dangling-ref scan globbed only SKILL.md, so 4 broken MOTION.md refs in supporting files passed 218/218. Also caught that ledger line 30's 'all 3 documents now agree' was inaccurate.
Fix wave (commit d9beb3c): all 6 findings + 2 spec gaps (apple-design, animation-vocabulary now wired per spec). 218→250 checks.
Fix wave part 2 (commit 71d1963, controller-found): d9beb3c fixed only the BARE-name branch of the scan; the slash-form branch still globbed SKILL.md, so design/MOTION.md in a supporting file matched NEITHER pattern. Re-running the reviewer's own mutation still passed 51/51. Both branches now glob skills/*/*.md — verified by breaking refs in states.md and intake.md.
Scoped re-review: all 8 findings ADDRESSED with independently-gathered evidence; 4 mutations across both ref forms and both file classes all caught; no new breakage. Residual deferred minors: 2, both already ruled shippable by the final review. NO load-bearing findings open.
